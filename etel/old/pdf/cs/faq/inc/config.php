<?php
// Created 2006-06-15 15:49:59

// Language-File
$PMF_CONF["language"] = "language_en.php";

// Enable automatic content negotiation
$PMF_CONF["detection"] = "TRUE";

// Title of the FAQ
$PMF_CONF["title"] = "Customer Service";

// FAQ Version
$PMF_CONF["version"] = "1.6.1";

// Describtion of the Page
$PMF_CONF["metaDescription"] = "";

// Keywords for Spiders
$PMF_CONF["metaKeywords"] = "Customer Service";

// Name of the Publisher
$PMF_CONF["metaPublisher"] = "";

// Email address of the Admin
$PMF_CONF["adminmail"] = "customerservice@etelegate.com";

// Contact information
$PMF_CONF["msgContactOwnText"] = "customerservice@etelegate.com";

// Copyright text for the startpage
$PMF_CONF["copyright_eintrag"] = "";

// Text for the send2friend page
$PMF_CONF["send2friend_text"] = "";

// maximum Size for attachments in Bytes (max. 16MByte)
$PMF_CONF["attmax"] = "100000";

// use Tracking?
$PMF_CONF["tracking"] = "TRUE";

// use Adminlog?
$PMF_CONF["enableadminlog"] = "TRUE";

// Do you want the IP to be checked when checking the UINs in admin.php?
$PMF_CONF["ipcheck"] = "TRUE";

// Number of displayed topics per page
$PMF_CONF["numRecordsPage"] = "10";

// Number of news articles
$PMF_CONF["numNewsArticles"] = "1";

// Ban these IPs
$PMF_CONF["bannedIP"] = "";

// Print user email in a safe way (default: enabled).
$PMF_CONF["spamEnableSafeEmail"] = "TRUE";

// Check public form content against banned words (default: enabled).
$PMF_CONF["spamCheckBannedWords"] = "TRUE";

// Use a catpcha code to allow public form submission (default: enabled).
$PMF_CONF["spamEnableCatpchaCode"] = "TRUE";

?>